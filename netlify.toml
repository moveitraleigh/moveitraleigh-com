[build]
  functions = "lambda"
  command = "hugo && env NSPC=$(echo $BRANCH | tr a-z A-Z) npm run build:lambda"

[context.phase3.environment]
  BRANCH_ENV = "staging"
  
[[redirects]]
  from = "/checkout"
  to = "/.netlify/functions/checkout"
  status = 200
